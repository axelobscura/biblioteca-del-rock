export default function Categorias({ result } : { result:any }) {

  console.log(result)

  return(
    <div className="flex flex-col items-center justify-center">
      <h1>CATEGORÍAS</h1>
    </div>
  )
}